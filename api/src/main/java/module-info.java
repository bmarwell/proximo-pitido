/**
 * Public API interfaces for Próximo Pitido.
 *
 * <p>This module defines the contracts used by both the SPI layer (language plug-ins)
 * and the WAR layer (SIP call handling), keeping them decoupled from each other.
 */
module de.bmarwell.proximo.pitido.api {
    exports de.bmarwell.proximo.pitido.api;
}
